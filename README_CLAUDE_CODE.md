# 🤖 미국 주식 뉴스 자동 분석 블로그 시스템 (Claude Code 버전)

**완전 무료 - API 비용 $0**

Claude Code를 직접 활용하여 뉴스를 수집, 분석하고 블로그 글을 작성하는 시스템입니다.

---

## 🎯 핵심 변경사항

### Before (API 방식)
- ❌ Claude API 사용 → 월 $25-100 비용
- ❌ 완전 자동화 but 품질 관리 어려움
- ❌ API 제한 및 토큰 사용량 관리 필요

### After (Claude Code 방식)
- ✅ Claude Code 직접 사용 → **완전 무료**
- ✅ 품질 관리 가능 (각 글 직접 확인)
- ✅ API 제한 없음

---

## 💰 비용 비교

| 항목 | API 방식 | Claude Code 방식 |
|------|----------|------------------|
| **Claude** | $25-100/월 | **$0** ⭐ |
| Finnhub | 무료 | 무료 |
| Alpha Vantage | 무료 | 무료 |
| Supabase | 무료 | 무료 |
| **총 비용** | **$25-100/월** | **$0/월** |

**절감액**: 연간 $300-1,200

---

## 🚀 빠른 시작 (5분)

### 1. 설치

```bash
cd /Users/jinxin/dev/stock-news-automation

# 의존성 설치 (Anthropic API 제외)
pip install -r requirements.txt

# 디렉토리 생성
mkdir -p logs prompts articles
```

### 2. 환경 설정

```bash
cp .env.example .env
nano .env
```

**.env 파일** (AI API 키 불필요!):
```env
# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your_supabase_anon_key

# News APIs (무료)
FINNHUB_API_KEY=your_finnhub_key
ALPHA_VANTAGE_API_KEY=your_alpha_vantage_key

# ✅ Claude API 불필요 - Claude Code 직접 사용!
```

### 3. Supabase 설정

```sql
-- Supabase SQL Editor에서 실행
-- database/schema.sql 내용 복사 후 실행
```

### 4. 첫 실행

```bash
# 뉴스 수집
python main.py --mode collect

# 프롬프트 생성
python scripts/generate_daily_workflow.py

# 워크플로우 확인
cat prompts/workflow_*.md
```

---

## 📋 일일 워크플로우

### Morning (10분) - 자동

```bash
# 1. 뉴스 수집
python main.py --mode collect

# 2. 워크플로우 프롬프트 생성
python scripts/generate_daily_workflow.py
```

**결과**:
- 최신 뉴스 수집 완료
- `prompts/analysis_*.md` 파일 생성 (분석용 프롬프트)
- `prompts/workflow_*.md` 파일 생성 (작업 가이드)

---

### Midday (30-45분) - Claude Code

```bash
# 분석 프롬프트 확인
cat prompts/analysis_20251112_0900.md
```

**Claude Code에서 작업**:
1. 프롬프트 내용을 Claude Code에 붙여넣기
2. Claude가 뉴스 분석
3. 결과를 JSON으로 받아 `prompts/analysis_results_*.json` 저장

```bash
# 분석 결과 저장
python scripts/save_analysis.py prompts/analysis_results_20251112_0900.json
```

---

### Afternoon (30-45분) - Claude Code

```bash
# 1. 트렌딩 종목 확인
python scripts/get_trending.py

# 2. 글 작성 프롬프트 생성
python scripts/generate_article_prompts.py AAPL TSLA NVDA
```

**Claude Code에서 작업**:
1. 각 종목 프롬프트를 Claude Code에 붙여넣기
2. Claude가 블로그 글 작성
3. 결과를 `articles/article_*.md` 로 저장

```bash
# 3. 작성한 글 발행
python scripts/publish_articles.py articles/article_*.md
```

---

## 🔄 시스템 플로우

```
┌─────────────────────────────────────────────┐
│  1. 뉴스 자동 수집 (15분마다)                │
│     Finnhub, Alpha Vantage, RSS             │
│     → Supabase 저장                          │
└──────────────────┬──────────────────────────┘
                   │ 자동
                   ↓
┌─────────────────────────────────────────────┐
│  2. 프롬프트 자동 생성                       │
│     python scripts/generate_daily_workflow.py│
│     → prompts/analysis_*.md 생성             │
└──────────────────┬──────────────────────────┘
                   │ 복사 & 붙여넣기
                   ↓
┌─────────────────────────────────────────────┐
│  3. Claude Code로 뉴스 분석 👤              │
│     프롬프트 → Claude Code → JSON            │
│     → prompts/analysis_results_*.json        │
└──────────────────┬──────────────────────────┘
                   │ 자동
                   ↓
┌─────────────────────────────────────────────┐
│  4. 분석 결과 저장                           │
│     python scripts/save_analysis.py          │
│     → Supabase 저장                          │
└──────────────────┬──────────────────────────┘
                   │ 자동
                   ↓
┌─────────────────────────────────────────────┐
│  5. 글 작성 프롬프트 생성                    │
│     python scripts/generate_article_prompts.py│
│     → prompts/article_*.md 생성              │
└──────────────────┬──────────────────────────┘
                   │ 복사 & 붙여넣기
                   ↓
┌─────────────────────────────────────────────┐
│  6. Claude Code로 블로그 글 작성 👤         │
│     프롬프트 → Claude Code → Markdown        │
│     → articles/article_*.md                  │
└──────────────────┬──────────────────────────┘
                   │ 자동
                   ↓
┌─────────────────────────────────────────────┐
│  7. 블로그 글 발행                           │
│     python scripts/publish_articles.py       │
│     → Supabase 저장 → WordPress (선택)      │
└─────────────────────────────────────────────┘
```

**자동화 비율**: 70% 자동 / 30% Claude Code 활용

---

## 📁 프로젝트 구조

```
stock-news-automation/
├── collectors/              # 뉴스 수집기 (자동)
│   ├── finnhub_collector.py
│   ├── alpha_vantage_collector.py
│   └── rss_collector.py
│
├── analyzers/              # 프롬프트 생성 (자동)
│   └── prompt_generator.py
│
├── scripts/                # 유틸리티 스크립트
│   ├── generate_daily_workflow.py    # 워크플로우 생성
│   ├── save_analysis.py              # 분석 결과 저장
│   ├── get_trending.py               # 트렌딩 종목
│   ├── generate_article_prompts.py   # 글 프롬프트
│   └── publish_articles.py           # 글 발행
│
├── prompts/                # 생성된 프롬프트 (자동)
│   ├── workflow_*.md
│   ├── analysis_*.md
│   └── article_*.md
│
├── articles/               # 작성된 글 (Claude Code)
│   └── article_*.md
│
├── database/               # Supabase 연동
│   ├── models.py
│   ├── supabase_client.py
│   └── schema.sql
│
├── main.py                 # 메인 실행 파일
└── CLAUDE_CODE_WORKFLOW.md  # 상세 워크플로우
```

---

## 🛠 주요 스크립트

### 1. 뉴스 수집
```bash
python main.py --mode collect
```

### 2. 워크플로우 생성
```bash
python scripts/generate_daily_workflow.py
```

### 3. 분석 결과 저장
```bash
python scripts/save_analysis.py prompts/analysis_results_20251112_0900.json
```

### 4. 트렌딩 종목 확인
```bash
python scripts/get_trending.py
```

### 5. 글 작성 프롬프트 생성
```bash
python scripts/generate_article_prompts.py AAPL TSLA NVDA
```

### 6. 블로그 글 발행
```bash
python scripts/publish_articles.py articles/article_*.md
```

---

## 💡 장점

### 1. 완전 무료
- Claude API 비용 $0
- Claude Code Pro 구독만으로 무제한 사용

### 2. 높은 품질
- 각 뉴스 분석 결과 직접 확인
- 블로그 글 품질 실시간 검토
- 원하는 스타일로 커스터마이징

### 3. 유연성
- 원하는 시간에 작업
- 일부만 분석/작성 가능
- 실시간 수정 및 개선

### 4. 학습 효과
- 주식 뉴스 분석 능력 향상
- 투자 인사이트 습득

---

## 📊 시간 vs 비용

| 방식 | 시간 투자 | 비용 | 품질 |
|------|----------|------|------|
| **완전 자동 (API)** | 10분/일 | $25-100/월 | 보통 |
| **Claude Code** | 60-90분/일 | **$0/월** | **높음** |

**Claude Code 방식 추천 대상**:
- ✅ 시간 여유가 있는 사람
- ✅ 품질을 중요시하는 사람
- ✅ 비용을 절감하고 싶은 사람
- ✅ 주식 분석 능력을 키우고 싶은 사람

---

## 🎯 예상 ROI

### 수익 모델
```
월간 방문자: 30,000명 (1,000/일)
페이지뷰: 75,000 (2.5 페이지/방문)
RPM: $10
월간 수익: $750

비용: $0 (Finnhub + Alpha Vantage + Supabase 무료)
순익: $750/월
```

### 손익분기점
- **필요 방문자**: 하루 0명 (비용이 $0이므로!)
- **목표**: 하루 100명부터 수익 발생

---

## 📚 문서

- **CLAUDE_CODE_WORKFLOW.md** - 상세 워크플로우 가이드
- **README.md** - 원본 시스템 문서 (API 방식)
- **QUICK_START.md** - 빠른 시작
- **SETUP_GUIDE.md** - 설치 가이드

---

## 🔧 커스터마이징

### 뉴스 수집 빈도 조정

```bash
# 크론탭 설정
crontab -e

# 매 30분마다 수집
*/30 * * * * cd /path/to/stock-news-automation && python main.py --mode collect
```

### 분석 프롬프트 템플릿 수정

```python
# analyzers/prompt_generator.py 수정
# generate_analysis_prompt() 함수에서 프롬프트 커스터마이징
```

### 글 작성 스타일 변경

```python
# analyzers/prompt_generator.py 수정
# generate_article_prompt() 함수에서 스타일 가이드 수정
```

---

## 🆘 문제 해결

### "프롬프트가 너무 깁니다"

```python
# scripts/generate_daily_workflow.py 수정
unanalyzed_news = self.db.get_unanalyzed_news(limit=20)  # 50 → 20
```

### "Claude Code 응답이 중단됩니다"

→ 배치 크기를 줄여서 한 번에 5-10개씩 분석

### "JSON 형식 오류"

```
User: JSON 형식이 잘못되었어. 다시 작성해줘.
      반드시 유효한 JSON 배열로만 출력해줘.
```

---

## 🚀 향후 개선 계획

### Phase 1 (현재)
- ✅ 뉴스 자동 수집
- ✅ 프롬프트 자동 생성
- ✅ Claude Code 워크플로우

### Phase 2
- [ ] 프롬프트 품질 개선
- [ ] 배치 처리 최적화
- [ ] WordPress 자동 발행

### Phase 3
- [ ] 소셜 미디어 자동 공유
- [ ] 이메일 뉴스레터
- [ ] 분석 통계 대시보드

---

## 💬 FAQ

**Q: API 방식과 Claude Code 방식 중 어느 것을 선택해야 하나요?**

A:
- **API 방식**: 완전 자동화, 시간 부족, 비용 부담 가능
- **Claude Code 방식**: 품질 중시, 시간 여유, 비용 절감 중요

**Q: 하루에 얼마나 시간이 걸리나요?**

A:
- 뉴스 수집: 자동 (0분)
- 뉴스 분석: 30-45분 (Claude Code)
- 글 작성: 30-45분 (Claude Code)
- **총: 60-90분/일**

**Q: 일부만 자동화할 수 있나요?**

A: 네!
- 뉴스 수집만 자동화하고 주말에 일괄 분석
- 분석까지만 하고 글은 나중에 작성
- 유연하게 조정 가능

---

## 🎉 결론

Claude Code를 활용한 무료 주식 뉴스 블로그 시스템:

- ✅ **완전 무료** ($0/월)
- ✅ **높은 품질** (직접 확인)
- ✅ **유연한 운영**
- ✅ **학습 효과**

**지금 바로 시작하세요!**

```bash
python main.py --mode collect
python scripts/generate_daily_workflow.py
cat prompts/workflow_*.md
```
